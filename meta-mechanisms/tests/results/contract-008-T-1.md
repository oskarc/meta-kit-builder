# contract-008 T-1 — the residue comparison, run 2026-09-16 on a fixture

Fixture: installed copies = the kit as installed; project copy of meta-map/SKILL.md carries two added lines; project copy of meta-antidrift/SKILL.md is untouched; the staged copies of both differ from the installed (line 3 reworded).

## A — installed copies present
- meta-map: differs from staged (4 lines), residue against installed = 2 line(s) -> one question, quoting:
      - A project rule kept here: every map entry names the file it loads, never a folder.
      - And a second: the map is re-read after every ratification pass.
- meta-antidrift: differs from staged (2 lines), residue against installed = 0 line(s) -> untouched, no question

## B — no installed copies; the kit's shipped line history stands in (meta-bootstrap/history/<skill>.lines, the same union of committed versions, generated by regenerate.sh — this run built the union directly from git; the shipped files are what the upgrade reads)
- meta-map: residue against history = 2 line(s) -> one question, quoting:
      - A project rule kept here: every map entry names the file it loads, never a folder.
      - And a second: the map is re-read after every ratification pass.
- meta-antidrift: residue against history = 0 line(s) -> untouched, no question

## C — neither installed copies nor history
- the classification says so and falls back to one question per differing skill (2 here) — the branch the text keeps for this case

Result: A and B give the same answer — one question, the two project lines quoted; the kit-only change in meta-antidrift raises none. C is the pre-contract-008 behaviour, stated rather than silent.
